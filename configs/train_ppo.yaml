experiment:
  seed: 42
  model_type: "PPO"
  semantic_mode: "action_prior"
  total_timesteps: 100000

ppo:
  learning_rate: 0.0003
  n_steps: 2048
  batch_size: 64
  n_epochs: 10
  gamma: 0.99
  device: "cpu"

env:
  max_steps: 50
  num_edge_servers: 3
