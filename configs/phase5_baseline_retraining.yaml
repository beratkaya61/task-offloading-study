training:
  base_config: "configs/synthetic_rl_training.yaml"
  algorithms: ["ppo", "dqn", "a2c"]
  seeds: [42, 43, 44]
  total_timesteps: 30000
  eval_episodes: 10

output:
  model_root: "models"
  report_path: "results/tables/offloading_experiment_report.md"
